# Геймплей и баланс

[Начало справочника](/Users/admin/IncrementalRPG/Docs/README.md) · [Анимации и интерфейс](/Users/admin/IncrementalRPG/Docs/PresentationSettings.ru.md) · [Точный каталог полей](/Users/admin/IncrementalRPG/Docs/SettingsCatalog.ru.md)

## 1. Где подключены игровые настройки

Откройте [GameScene](/Users/admin/IncrementalRPG/Assets/Scenes/GameScene.unity), объект `--Reflex--/SceneScope`, компонент **GameSceneInstaller**. Здесь назначены используемые `Dungeon List`, `Damage Zone Config`, `Bomb Explosion Config`, `Shard Pickup Config`, `Skill Tree Config`, `Item Catalog` и настройки перехода `Level Transition Config`.

Наличие нового asset в папке ещё не включает его в игру. Ноду нужно добавить в `SkillTreeConfig.entries`, уровень — в `DungeonConfig.levels`, подземелье — в `DungeonList.dungeons`, существо — в таблицу спавна уровня, предмет — в каталог и нужный пул наград.

## 2. Дерево прокачки

Файлы: [общий конфиг дерева](/Users/admin/IncrementalRPG/Assets/SO/SkillTree/SkillTreeConfig.asset), [ноды](/Users/admin/IncrementalRPG/Assets/SO/SkillTree/Nodes). В Project можно искать по типам `t:NodeDefinition`, `t:SkillTreeConfig`. Встроенный редактор доступен через **Tools → Skill Tree → Editor** или **Open Skill Tree Editor** у конфига. `Add Selected Nodes` добавляет выбранные в Project ноды, `Create Node` создаёт новую, `Frame All` показывает дерево целиком. `Remove From Tree` убирает запись из дерева, а `Delete Asset` удаляет сам asset.

### Поля одной ноды

| Поле | Что меняет |
|---|---|
| `id` | Уникальный ключ ноды в сохранении. Не повторять у двух нод |
| `displayName`, `description` | Локализованные название и описание в окне ноды |
| `icon`, `additionalIcon` | Основная и дополнительная маленькая иконки |
| `maxLevel` | Количество покупок ноды, минимум 1 |
| `goldCostPerLevel` | Цена каждой очередной покупки. Элемент 0 — переход 0→1, элемент 1 — 1→2 |
| `prerequisites[].node` | Предыдущая нода |
| `prerequisites[].requiredLevel` | Сколько уровней предыдущей ноды нужно купить. Для покупки должны выполняться **все** зависимости |
| `effects` | Один или несколько эффектов покупки |
| `effects[].effectType` | `Additive`, `Multiplicative` или `FeatureUnlock` |
| `effects[].statType` | Изменяемая характеристика, таблица ниже |
| `effects[].valuesPerLevel` | **Прибавка за каждую покупку**, а не итог на этом уровне. Индекс 0 соответствует первой покупке |
| `effects[].feature` | Открываемая механика для `FeatureUnlock`; у двух других типов игнорируется |

Заполняйте цену и значения эффекта на все `maxLevel` уровней. В текущей реализации отсутствующая цена делает покупку бесплатной, а недостающий элемент эффекта не даёт прибавки. Лишние элементы за пределами `maxLevel` не участвуют.

`Additive` складывает значения купленных уровней в сумму **A**. `Multiplicative` тоже сначала складывает доли в **P**, затем даёт множитель **1 + P**. Два бонуса `0.1` дают множитель `1.2`, а не `1.21`. Название типа эффекта не гарантирует, что выбранная характеристика его использует: ниже перечислены реально подключённые варианты.

### Все характеристики дерева и реальные формулы

Обозначения: **B** — базовое значение; **A** — сумма `Additive`; **P** — сумма `Multiplicative`. Отрицательные/слишком маленькие результаты некоторые потребители ограничивают; допустимые рабочие значения приведены в описаниях.

| `StatType` | Рабочие типы эффектов | Результат и единицы |
|---|---|---|
| `ManualAttackDamage` | Additive + Multiplicative | `(B + A) × (1 + P)`, урон ручного удара; результат округляется |
| `AutoAttackDamage` | Additive + Multiplicative | Та же формула для автоатаки |
| `SpecialAttackDamage` | Additive + Multiplicative | Та же формула для особой атаки |
| `ManualAttackSpeed` | Additive + Multiplicative | Интервал = `max(0.05, max(0.05, B) / max(0.01, (1 + A) × (1 + P)))`, с |
| `AutoAttackSpeed` | Additive + Multiplicative | Та же формула для периода автоатаки |
| `ZoneRadius` | **Multiplicative** | Горизонтальный радиус = `B × (1 + P)`; Additive здесь не читается |
| `SpawnSpeed` | **Additive** | Интервал = `max(B × (1 − A), minSpawnInterval)`; `0.1` уменьшает исходный интервал на 10% |
| `MapSize` | **Additive** | Сторона квадратной сетки = `minPlayZoneSize + (int)A`; дробная часть отбрасывается |
| `SessionTime` | **Additive** | Дополнительные секунды к времени уровня |
| `BombExplosionRadius` | Additive + Multiplicative | `B × (1 + P) + A`, мировые единицы; абсолютная прибавка применяется после множителя |
| `BombExplosionDamage` | Additive + Multiplicative | `(B + A) × (1 + P)`, целый урон взрыва |
| `BombSpawnSpeed` | **Additive** | `max(B × (1 − A), minBombSpawnInterval)`; отдельный таймер бочек |
| `GoldDrop` | **Multiplicative** | Золото существа × `goldDropMultiplier` уровня × `(1 + P)`, с округлением |
| `InitialEnemySpawnDensity` | **Additive** | Прибавка к доле клеток, заполняемых обычными существами при старте |
| `InitialBombSpawnDensity` | **Additive** | Прибавка к доле клеток для стартовых бочек |
| `SlimeShardDrop` | **Multiplicative** | Базовые осколки слизня × `(1 + P)`, с округлением |
| `SkeletonShardDrop` | **Multiplicative** | Аналогично для скелета |
| `DemonShardDrop` | **Multiplicative** | Аналогично для демона |
| `CrystalShardDropBonus` | **Additive** | Абсолютная прибавка к осколкам кристалла |
| `ShardPickupSpeed` | Additive + Multiplicative | Время сбора = `max(0.01, B / max(0.01, (1 + A) × (1 + P)))`, с |

Неперечисленный для строки тип эффекта не используется этой механикой, даже если его можно выбрать в Inspector. Например, процентный радиус зоны задаётся через `Multiplicative`, а уменьшение интервала спавна — через `Additive` с долями.

`FeatureUnlock` открывает `Bombs`, `AutoAttack`, `SpecialAttack` или `Shards` после первой покупки. `None` не открывает игровую механику. У ручной атаки отдельный unlock не требуется. Скорость особой атаки отдельным `StatType` пока не представлена: её задержка меняется в базовом конфиге.

### Положение, связи и видимость

| Поле общего конфига | Назначение |
|---|---|
| `entries[].node` | Нода, включённая в активное дерево |
| `entries[].gridPosition` | Целочисленные X/Y на сетке |
| `cellSpacing` | Расстояние между клетками по X/Y в UI; сейчас 150×150 |
| `origin` | Смещение начала координат |

Линии строятся по `prerequisites`, а положение — по `entries`. Старое скрытое поле `positionInGraph` не управляет текущим деревом при заполненных `entries`.

До первой покупки видна стартовая нода. Далее видимость смотрит на **первую** зависимость и её первую зависимость: если в родителя или такого прародителя вложен хотя бы один уровень, нода видна. Поэтому показываются следующие ноды и ещё слой заблокированного продолжения. Для покупки учитываются **все** родители и их `requiredLevel`.

Полная прокачка — зелёная, доступная покупка — жёлтая, нехватка золота — красная; заблокированные требования имеют отдельное состояние `Locked` и анимацию замка. Конкретные цвета/спрайты задаются в `ViewConfigs`, анимация раскрытия — в `SkillTreeView` (руководство аниматора).

Существующие ноды урона усиливают ручную атаку. Отдельные характеристики авто/особой атаки уже поддерживаются, но отдельные ноды их урона в текущем дереве не размещены. Для новой ноды достаточно конфига с соответствующим `StatType` и записи в `entries`.

Три ноды частоты спавна размещены после `AddZoneDamageNode 3`: координаты (10,3), (12,3), (14,3). Первая требует 5 уровней этой ноды урона; последующие требуют по 1 уровню своей предыдущей ноды. Имена файлов в цепочке: `AddSpawnSpeedNode` → `AddSpawnSpeedNode 2` → `AddSpawnSpeedNode 1`.

### Пример настройки

Нужно +10% ручного урона за каждую из трёх покупок: `maxLevel = 3`, три цены, эффект `Multiplicative`, характеристика `ManualAttackDamage`, массив `0.1, 0.1, 0.1`. Итог после третьей покупки — +30% к базе с абсолютными прибавками. Для автоатаки поменяйте только характеристику на `AutoAttackDamage`.

## 3. Атаки и дамаг-зона

Asset: [DamageZoneConfig](/Users/admin/IncrementalRPG/Assets/SO/DamageZone/DamageZoneConfig.asset).

| Поле | Что меняет | Текущее значение asset |
|---|---|---|
| `baseManualAttackDamage` | Базовый урон ручного удара | 1 |
| `baseManualAttackCooldown` | Минимальное время между ручными ударами до прокачки, с | 0,75 |
| `baseAutoAttackDamage` | Базовый урон одного автоудара | 1 |
| `baseAutoAttackInterval` | Период автоатаки до прокачки, с | 0,75 |
| `baseSpecialAttackDamage` | Базовый урон особой атаки | 5 |
| `baseSpecialAttackCooldown` | Перезарядка особой атаки, с | 5 |
| `baseRadius` | Горизонтальный радиус общей зоны, мировые единицы | 1 |
| `aspectRatio` | Вертикальный радиус / горизонтальный радиус | 0,55 |

Все три вида атаки используют одну игровую область. Вертикальный радиус равен горизонтальному × `aspectRatio`. Урон и бонусы разных атак независимы. Увеличение скорости ручной атаки/автоатаки делит базовый интервал; +100% скорости даёт вдвое меньший интервал.

Графика настраивается отдельно в [DamageZone.prefab](/Users/admin/IncrementalRPG/Assets/Prefabs/DamageZone.prefab). Поле `DamageZoneView._baseRadiusX` калибрует изображение относительно радиуса и не является базовым игровым радиусом. Детали трёх анимаций атак — в руководстве аниматора.

## 4. Подземелья, уровни, время и прогресс

Корневой список: [DungeonList](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/DungeonList.asset). Текущий игровой биом: [0_DungeonConfig](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/0_DungeonConfig.asset). Конфиги уровней: [Levels](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/Levels).

`DungeonConfig`: `dungeonId` — ключ прогресса; `displayName`, `title`, `description` — локализованный текст; `icon`, `previewImage` — изображения; `levels` — последовательность уровней. Фактический вывод текстов/картинок зависит от представления: наличие поля не означает, что оно показано текущим экраном. Уровень считается пригодным для запуска при назначенных `spawnTable` и `tilemapGenerationConfig`.

| Поле `DungeonLevelConfig` | Назначение |
|---|---|
| `levelId` | Идентификатор уровня |
| `displayName`, `title`, `description` | Тексты уровня |
| `xpGoal` | Опыт для завершения уровня; это не фиксированное число убийств |
| `tilemapGenerationConfig` | Набор правил генерации поверхности |
| `minPlayZoneSize` | Базовая длина стороны сетки, в клетках, до бонуса `MapSize` |
| `heatIndex` | Жар: больше значение — меньше базовое время до заполнения лавой |
| `spawnTable` | Какие существа, кристаллы и бочки разрешены на уровне |
| `initialEnemySpawnDensity` | Начальная плотность обычных существ, доля от количества клеток |
| `initialBombSpawnDensity` | Начальная плотность бочек |
| `spawnInterval`, `minSpawnInterval` | Базовый и минимальный период общего спавна, с |
| `bombSpawnInterval`, `minBombSpawnInterval` | Базовый и минимальный период бочек, с |
| `goldDropMultiplier` | Множитель золота с существ на этом уровне |
| `lootPool.entries[].item` | Предмет в награде за переход |
| `lootPool.entries[].weight` | Относительный вес этого предмета |

Текущая формула времени уровня:

`100 / max(heatIndex, 0.0001) − ArmorIndex / 2.5 + Additive(SessionTime)` секунд.

Знак перед бронёй в реализации **минус**. `ArmorIndex` пока берётся из состояния игрока, а не из экипированных предметов. Его начальное значение 1 задано в коде. Это описание текущей формулы, а не рекомендация баланса.

При достижении `xpGoal` работает окно добора осколков `lootGraceDuration`, затем переход. На последнем доступном уровне показывается предусмотренный игрой экран окончания демо; отдельного числового «лимита демо» в конфиге нет. Порядок и наличие пригодных уровней определяются массивом `levels`.

## 5. Спавн врагов, кристаллов и бочек

Таблица спавна — asset типа `SpawnTable`, назначенный именно в нужном уровне. В биоме есть несколько файлов `0_*SpawnRule`: ориентируйтесь на ссылку `spawnTable`, а не только на имя файла.

| Поле записи таблицы | Назначение |
|---|---|
| `entries[].config` | `EntityConfig` существа/бочки/кристалла |
| `entries[].weight` | Относительная вероятность среди подходящих записей; нулевой вес исключает запись |
| `entries[].requiredFeature` | Требуемая разблокировка дерева; `None` — без требования |

Шанс конкретной подходящей записи = её вес / сумма весов подходящих записей. Веса не обязаны суммироваться до 100.

**Общий периодический спавн** выбирает из всех подходящих записей, кроме `FeatureType.Bomb`: это обычные враги и, если разрешены таблицей, кристаллы. Поэтому характеристика `SpawnSpeed` сейчас ускоряет общий поток без бочек, а не отдельный таймер каждого вида врага.

**Бочки** имеют отдельный период и требуют открытия `Bombs`. Дополнительной проверки шанса бочки после таймера нет. Но нужны свободная клетка и подходящая запись бочки в таблице. Веса нескольких бочек выбирают вариант бочки, а не вероятность пропуска появления. Первое периодическое появление ждёт интервал; немедленные стартовые бочки задаются плотностью.

Начальное заполнение: `round(число клеток × max(0, плотность уровня + A))`. Обычная стартовая плотность выбирает `FeatureType.None`; стартовые бочки — `FeatureType.Bomb`. Это одноразовое заполнение при старте, а не частота дальнейшего появления. Карта ограничивает число реально размещаемых объектов.

Базовый период — задержка до прокачки. Минимальный — нижний предел после прокачки. Пример: база 2 с, минимум 0,5 с, сумма `SpawnSpeed = 0.25` → итог 1,5 с. Если минимум равен базе или выше неё, бонус не ускоряет спавн. В текущих пяти уровнях RocksAndLava такой предел уже достигнут; значения приведены в каталоге как факт текущей настройки.

У новых полей интервала бочек исходные значения в классе — 10 с и 0,5 с. В существующих YAML уровней эти поля на момент документации ещё не записаны явно. Проверяйте значение в Inspector перед изменением и сохранением конкретного уровня.

## 6. Существа, кристаллы, бочки

Ассеты: [Entities текущего биома](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/Entities). Каждый — `EntityConfig`; визуальная часть указана в `viewPrefab`.

| Поле | Значение для настройки |
|---|---|
| `entityName`, `description`, `icon` | Имя, подсказка и иконка существа |
| `maxHP` | Здоровье; минимум 1, целое число |
| `entityKind` | Вид для выбора бонуса осколков: None / Slime / Skeleton / Demon / Crystal |
| `featureType` | Участие в потоках спавна: None — обычный объект, Bomb — бочка, Crystal — кристалл |
| `viewPrefab` | Префаб графики и компонентов существа |
| `goldDrop` | Базовое золото за уничтожение, до множителей уровня и дерева |
| `shardDrop` | Базовые осколки до видовой прокачки |
| `xpReward` | Опыт за убийство, учитываемое как враг |
| `countsAsEnemyKill` | Включать в счётчик убитых врагов и событие врага, от которого начисляются золото/опыт |
| `canMove` | Разрешить движение по сетке |
| `moveChance` | Вероятность начать движение при очередной проверке, 0…1 |
| `moveCheckInterval` | Период проверок движения, с |
| `moveSpeed` | Переходы в соседние клетки в секунду, включая диагональные |
| `damageZoneHitRadius` | Радиус игровой области попадания существа, мировые единицы |
| `damageSound` | Звук попадания по этому типу |
| `deathSounds` | Набор вариантов звука смерти |
| `drawDamageZoneHitAreaGizmo` | Показывать отладочную область попадания в редакторе |
| `damageZoneHitAreaGizmoColor` | Цвет этой отладочной области |

`entityKind` и `featureType` решают разные задачи. Для бочки HP берётся из её `EntityConfig`, а урон взрыва — из `BombExplosionConfig`. `moveSpeed` перемещает объект и не ускоряет Spine-клип ходьбы автоматически.

`countsAsEnemyKill` влияет на золото и опыт через событие `OnEnemyKilled`. Система осколков слушает уничтожение сущности отдельно и дополнительно проверяет вид/разблокировку. Поэтому не стоит считать этот флаг общим выключателем всех наград.

## 7. Взрыв бочки

Asset: [BombExplosionConfig](/Users/admin/IncrementalRPG/Assets/SO/BombExplosionConfig.asset).

| Поле | Что меняет | Сейчас |
|---|---|---|
| `baseDamage` | Урон взрыва до прокачки | 10 |
| `baseRadius` | Горизонтальный радиус взрыва | 3 |
| `aspectRatio` | Отношение вертикального радиуса к горизонтальному | 0,55 |

Ноды радиуса используют `BombExplosionRadius`, ноды урона — `BombExplosionDamage`. Они независимы. Частота появления относится к уровню, а не к этому asset. Визуальное увеличение взрыва на префабе обслуживает `BombExplosionVisualScaler`; подробности — в анимациях.

## 8. Генерация карты, камера и лава

Ассеты: [правила генерации](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/DungeonRules/0_TilemapGenerationConfig.asset), [тайлсет](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/DungeonRules/0_TilemapTileSet.asset).

| `TilemapGenerationConfig` | Что меняет |
|---|---|
| `tileSet` | Набор напольных и боковых тайлов |
| `gradientDirection` | Направление перехода между областями: четыре осевых и две диагонали |
| `gradientRemap` | Кривая преобразования градиента 0…1; перераспределяет долю визуальных областей |
| `seed` | Воспроизводимый рисунок шума и вариантов тайлов |
| `noiseScale` | Частота неровностей границы: меньше — плавнее, больше — мельче |
| `noiseBorderStrength` | Сила смещения границы шумом; 0 — ровная |
| `pillarHeight` | Количество слоёв боковых стен под платформой; 0 отключает их |

`TilemapTileSet.tileRules[]`: базовый `tile`, диапазон `gradientMin`/`gradientMax`, список `variants[]` с `tile` и `probability`. Диапазон определяет положение/ширину области, код выбирает правило с наибольшим весом близости к середине диапазона. Вероятности вариантов суммируются в порядке списка; оставшаяся вероятность возвращает базовый тайл. Сумма больше 1 обрезает доступ поздним вариантам. `leftWallTile`, `rightWallTile` задают боковые грани.

В сцене объект `MapGenerator`, компонент `IsometricGradientTilemapGenerator`: `targetTilemap`, `leftPillarTilemap`, `rightPillarTilemap` — поверхности; `cameraAutoFitter` — подгонка камеры; `origin` — начало сетки; `autoFitCameraAfterGenerate` — подгонять камеру после генерации. `size` и `config` пригодны для редакторского предпросмотра через контекстное меню **Generate**, но при запуске уровня заменяются значениями уровня и дерева. Изменять игровой размер нужно в `minPlayZoneSize`/`MapSize`.

В сцене объект `TilemapCameraAutoFitter`, одноимённый компонент:

| Поле | Что меняет |
|---|---|
| `targetTilemap`, `targetCamera` | Источник границ и камера |
| `viewportFill` | Доля экрана, занимаемая картой; меньше — шире поля вокруг |
| `minOrthographicSize`, `maxOrthographicSize` | Пределы размера ортографической камеры |
| `keepCurrentCameraZ` | Сохранять глубину камеры при подгонке |
| `normalizedVerticalOffset` | Вертикальное смещение: фактически множитель текущего `orthographicSize`; положительное поднимает камеру |
| `lavaRoot` | Общий объект оформления лавы |
| `lavaReferenceOrthoSize` | Эталонный размер камеры для масштаба лавы |
| `lavaVerticalOffsetFixed` | Постоянное смещение лавы по Y, мировые единицы |
| `lavaVerticalOffsetScaled` | Смещение Y, умножаемое на масштаб лавы |
| `lavaOffScreenMargin` | Насколько ниже нижнего края камеры начинается подъём лавы |

Скорость подъёма лавы связана с оставшимся временем уровня, а скорость собственных Spine-анимаций лавы — отдельная визуальная настройка. Число `heatIndex` меняет время игры, а не скорость волн внутри клипа.

## 9. Осколки: количество, разлёт и сбор

Asset: [ShardPickupConfig](/Users/admin/IncrementalRPG/Assets/SO/Shard/ShardPickupConfig.asset); представление: [ShardPickup.prefab](/Users/admin/IncrementalRPG/Assets/Prefabs/Shard/ShardPickup.prefab).

| Поле | Назначение |
|---|---|
| `pickupPrefab`, `icon` | Представление подбираемого осколка |
| `basePickupValue` | Номинал для расчёта количества видимых предметов; не заменяет сумму награды существа |
| `maxPickupCount` | Максимум видимых осколков одного выпадения |
| `lifetime` | Время жизни на поле, с |
| `hitRadius` | Радиус попадания осколка в область сбора |
| `scatterDuration` | Длительность разлёта, с |
| `minScatterDistance`, `maxScatterDistance` | Диапазон дальности разлёта, мировые единицы |
| `baseCollectionDuration` | Базовое время сбора в зоне до `ShardPickupSpeed`, с |

Число визуальных предметов считается по базовой награде и номиналу, ограничивается `maxPickupCount`; итоговая награда распределяется между ними. Повышение номинала уменьшает визуальное количество и не должно использоваться для изменения общего дропа. Сам дроп задаётся в `EntityConfig.shardDrop` и нодах вида. Механика требует открытия `Shards`.

Подъём при сборе/блеск осколка и полёт к счётчику HUD имеют отдельные настройки/константы, перечисленные в руководстве аниматора.

## 10. Предметы, награды, инвентарь

Ассеты: [предметы и каталог](/Users/admin/IncrementalRPG/Assets/SO/Items). Поле `ItemCatalog._items` связывает определения предметов с игровым хранилищем.

| `ItemDefinition` | Назначение |
|---|---|
| `itemId` | Уникальный идентификатор предмета |
| `displayName`, `description`, `icon` | Резервные название/описание и иконка |
| `localizedName`, `localizedDescription` | Локализованные название/описание; если ссылка не задана, используются резервные строки |
| `category` | Misc / Armor / Consumable / Scroll / Currency; определяет доступные действия |
| `stackable`, `maxStackSize` | Можно ли складывать, максимум в стопке |
| `width`, `height` | Размер предмета в клетках инвентаря |
| `sellPrice` | Цена продажи/переработки, BigDouble |
| `rarity` | Common / Rare / Unique / Legendary |
| `equipmentSlot` | Для брони: Helmet / Chest / Weapon. Boots не является рабочим слотом |
| `defaultStats[].statId`, `value` | Выбор одной из семи характеристик и бонус долей: 0.10 = +10% |
| `effectId`, `effectValue` | Идентификатор и сила эффекта расходника |
| `forgeModifierId`, `forgeModifierValue` | Данные модификатора свитка для кузницы |
| `currency` | Для Currency: Gold / Shards. Сумма задаётся в записи пула уровня |

Для брони размер определяется слотом: **шлем 1×1**, нагрудник 2×2, оружие 1×2; броня не складывается в стопки. Для остальных предметов `stackable = true` задаёт размер 1×1. Для нестакуемого предмета `maxStackSize` равен 1. Редкость, цена и характеристики фиксируются при создании экземпляра: последующие изменения `rarity`, `sellPrice` и `defaultStats` в определении влияют на новые экземпляры, но не перезаписывают значения уже созданных.

<a id="equipment-stats"></a>

### Экипировка: семь процентных характеристик

Настройка: `ItemDefinition → Armor → Default Stats`. В `Stat Id` выбирается значение из списка, в `Value` вводится доля бонуса (`0.10` = +10%). Допустимые ID заданы в [EquipmentStats.cs](/Users/admin/IncrementalRPG/Assets/Scripts/Core/Items/EquipmentStats.cs). Неизвестные ID, повтор одного ID внутри предмета, отрицательные и неконечные значения недопустимы: Inspector сообщает ошибку настройки, создание и экипировка такой брони отклоняются.

| Stat Id | Действие после тренировки |
|---|---|
| `manual_attack_damage` | Урон только ручной атаки × (1 + сумма бонусов) |
| `auto_attack_damage` | Урон только автоатаки × (1 + сумма бонусов) |
| `manual_attack_speed` | Интервал ручной атаки / (1 + сумма бонусов) |
| `auto_attack_speed` | Интервал автоатаки / (1 + сумма бонусов) |
| `player_zone_radius` | Радиус зоны × (1 + сумма бонусов); обе оси и визуальный круг |
| `barrel_explosion_damage` | Урон взрыва бочки × (1 + сумма бонусов) |
| `barrel_explosion_radius` | Радиус взрыва × (1 + сумма бонусов); область поражения и визуальный масштаб |

Бонусы одинаковой характеристики на **надетых** шлеме, нагруднике и оружии складываются; одновременно действует по одному предмету каждого слота. Для урона ручной и автоматической атак: `(база + плоская прибавка тренировки) × множитель тренировки × (1 + сумма экипировки) × множитель зелья`, затем округление до целого. Пример: урон после тренировки 100, два предмета по +10% → 120, ярость +25% → 150. Особая атака не получает ни урона, ни скорости от этих семи бонусов, но на неё действуют зелья ярости и концентрации. Ярость не усиливает взрыв бочки.

Частоты ручной и автоматической атак независимы. Итоговый интервал: `max(0.05, max(0.05, база) / скорость тренировки / (1 + сумма бонусов скорости))`. База ручной атаки — `baseManualAttackCooldown = 0.75 с` в [DamageZoneConfig.asset](/Users/admin/IncrementalRPG/Assets/SO/DamageZone/DamageZoneConfig.asset:18). Нижний предел 0.05 с задан в коде `DamageZone.MinAttackInterval`. Бонус радиуса относится к радиусу, а не площади.

Визуальные настройки и локализация — [интерфейс экипировки](/Users/admin/IncrementalRPG/Docs/PresentationSettings.ru.md#equipment-inventory).

<a id="potion-test-settings"></a>

### Зелья: настройки, формулы и применение

В Inspector нужного `ItemDefinition` откройте секцию **Consumable → Effect Value** (`effectValue`). Число задаётся долей: `0.25` = 25%, `0.5` = 50%, `0.2` = 20%. Ввод `25` означает 2500%. Ниже указаны текущие значения ассетов.

| Зелье / конфиг | `itemId` | `effectId` — семейство | `effectValue` | Действие |
|---|---|---|---|---|
| [Богатства](/Users/admin/IncrementalRPG/Assets/SO/Items/WealthPotion.asset:49) | `potion_wealth` | `creature_gold_bonus` | `0.25` | +25% золота с существ |
| [Большое богатства](/Users/admin/IncrementalRPG/Assets/SO/Items/LargeWealthPotion.asset:49) | `potion_wealth_large` | `creature_gold_bonus` | `0.5` | +50% золота с существ |
| [Ярости](/Users/admin/IncrementalRPG/Assets/SO/Items/RagePotion.asset:49) | `potion_rage` | `player_attack_damage_bonus` | `0.25` | +25% урона ручной, автоматической и особой атак |
| [Большое ярости](/Users/admin/IncrementalRPG/Assets/SO/Items/LargeRagePotion.asset:49) | `potion_rage_large` | `player_attack_damage_bonus` | `0.5` | +50% урона тех же трёх атак |
| [Концентрации](/Users/admin/IncrementalRPG/Assets/SO/Items/ConcentrationPotion.asset:49) | `potion_concentration` | `special_attack_cooldown_reduction` | `0.2` | Время перезарядки особой атаки на 20% короче |

**Формулы.** Урон и золото округляются до целого после всех множителей:

- Богатство: `goldDrop существа × max(0, goldDropMultiplier уровня × множитель GoldDrop дерева) × (1 + effectValue)`. Продажа предметов/переработка, осколки и другие источники дохода не меняются.
- Ярость: `урон соответствующей атаки после тренировки и экипировки × (1 + effectValue)`. Каждый из трёх видов атак использует собственную базу и характеристику тренировки; экипировка усиливает только ручную и автоматическую атаки. Урон взрыва бочки не меняется.
- Концентрация: `max(0, baseSpecialAttackCooldown) × (1 − effectValue)`. Хранится **доля сокращения длительности**, а не скорость восстановления: 10 с при `0.2` становятся 8 с, не `10 / 1.2`. Ручная и автоматическая атаки не ускоряются. Зелье не открывает особую атаку, если она ещё не изучена.

Для богатства и ярости задавайте конечное неотрицательное число. Для концентрации допустимо `0 ≤ effectValue < 1`. Inspector не ограничивает `effectValue`, но недопустимое значение или неизвестный `effectId` блокируют применение без списания предмета. Уже подготовленный эффект сохраняет свою силу, даже если позже изменить конфиг.

Общие поля пяти ассетов:

| Поле Inspector / имя в данных | Значение и назначение |
|---|---|
| Category / `category` | `Consumable` (2) |
| Stackable / `stackable`; Max Stack Size / `maxStackSize` | `true`; 99 — стопка предметов в инвентаре, не суммирование активных эффектов |
| Width, Height / `width`, `height` | 1 × 1 клетка |
| Sell Price / `sellPrice` | 0; цена продажи задаётся отдельно от силы эффекта |
| Rarity / `rarity`; Equipment Slot / `equipmentSlot` | `Common`; `None` |
| Default Stats / `defaultStats` | Пусто; это не предметы брони |
| Icon / `icon` | Существующие спрайты семейств из `Assets/Art/UI/NEW/зелья и шарды`; обычные зелья — версии `_small`, большие — без суффикса |
| Localized Name / `localizedName`; Localized Description / `localizedDescription` | Ссылки на записи `LocalesTable`; описание получает силу как аргумент `{0}` |
| Display Name / `displayName`; Description / `description` | Резервные строки для предметов без ссылок локализации. У пяти зелий UI использует локализованные поля |

**Ограничения применения, заданные в коде.** Одно применение расходует один предмет и готовит эффект к ближайшему следующему уровню. Эффект действует от начала до завершения этого уровня. Одновременно допускается по одному эффекту семейства богатство / ярость / концентрация; разные семейства могут действовать вместе. Обычная и большая версии одного семейства не совмещаются и не заменяют друг друга. Пока семейство подготовлено или активно, повторное применение отклоняется без списания.

При поражении, достижении конца демо или досрочном выходе отменяются активные и подготовленные эффекты. Подготовленные до старта эффекты переживают перезапуск игры; активные не переносятся в следующий запуск, потраченный предмет не возвращается.

**Каталог и награды.** Ссылки на определения зелий задаются в [ItemCatalog._items](/Users/admin/IncrementalRPG/Assets/SO/Items/ItemCatalog.asset:15). `itemId` различает предметы; общий `effectId` объединяет обычное и большое зелье в семейство. Сохраняйте идентификаторы при настройке силы.

В уровнях биома Rocks and Lava выпадение задаётся через `DungeonLevelConfig.lootPool.entries[].weight`. Таблица ниже показывает относительные веса, не проценты вероятности; сумма весов не обязана равняться 100. Большие версии появляются с награды за третий уровень. Пул пятого уровня настроен, хотя текущий конец демо не выдаёт обычный лутбокс перехода.

| Пул завершённого уровня | Богатство | Большое богатство | Ярость | Большая ярость | Концентрация | Суммарно зелья |
|---|---:|---:|---:|---:|---:|---:|
| [01](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/Levels/0_Level_01.asset) | 14 | 0 | 14 | 0 | 7 | 35 |
| [02](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/Levels/0_Level_02.asset) | 12 | 0 | 12 | 0 | 6 | 30 |
| [03](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/Levels/0_Level_03.asset) | 6 | 4 | 6 | 4 | 5 | 25 |
| [04](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/Levels/0_Level_04.asset) | 5 | 4 | 5 | 4 | 4 | 22 |
| [05](/Users/admin/IncrementalRPG/Assets/SO/Dungeons/0_RocksAndLava/Levels/0_Level_05.asset) | 4 | 4 | 4 | 4 | 4 | 20 |

**HUD и локализация.** Оформление активных эффектов в `GameScene → HUD/HUD_Panel/PotionHud` описано в [руководстве интерфейса](/Users/admin/IncrementalRPG/Docs/PresentationSettings.ru.md#potion-hud). Подготовленные эффекты в этом списке не показываются.

Названия и описания пяти зелий используют `item.<itemId>.name` и `item.<itemId>.description` в [LocalesTable](/Users/admin/IncrementalRPG/Assets/Locales), языки `ru-RU`, `en-US`, `zh`. Строки величины — `potion.effect.wealth`, `.rage`, `.concentration`; сообщения применения — `potion.use.*`; кнопки и статус награды — `loot.*`. Шаблон `{0:P0}` форматирует долю из `effectValue` как процент для выбранного языка; числовой бонус в переводе задавать не нужно.

### Сетка инвентаря

`GameScene → MenuCanvas/InventoryView`, компонент `PlayerInventoryView`, ссылка `Runtime Inventory` ведёт к `UniversalInventory` на контейнере слотов. В текущей сцене: 36 слотов, 9 столбцов × 4 строки.

- `UniversalInventory._initialSlotCount`: число слотов.
- `_useGridTopology`, `_gridTopology._columns`, `_gridTopology._rows`: форма сетки для размещения предметов. Согласуйте с числом слотов и визуальным `GridLayoutGroup` контейнера.
- `baseSlotPrefab`: оформление клетки; `_slotContainer`: контейнер клеток.
- `_inventoryStrategy`: перенос и объединение стопок; текущая `AutoMergeSeparableStacksStrategy` использует `_dragAmount`, `_customDragAmount`, `_maxStackSize` и `_allowItemStackOverride`. Максимум стратегии сейчас 99; разрешено использовать индивидуальный предел предмета.
- `_slotManagementSettings`: политика количества слотов, сейчас фиксированная.
- `_ruleValidator`: дополнительные правила допустимости предметов.
- `_dropPolicy`: поведение при занятом месте, альтернативном размещении, обмене и частичном переносе. Это правила управления, а не характеристики предмета.
- `_shapedPlacementAnchorStrategy`: положение многоклеточного предмета относительно захваченной клетки.
- `PlayerInventoryView`: ссылки на три рабочих слота экипировки, декоративный силуэт Boots без drop/equip-механики, переработку, канвасы перетаскивания/подсказки и боковое меню.

Цвет отображения размещения — `PlacementOverlay._color`, префаб отображаемой иконки — `_itemPrefab`. Внешний вид drag — `InventoryDragVisualBinder._dragVisualPrefab`. Эти настройки не изменяют размеры предмета в клетках.

### Лутбокс и награда

Пул определяется в `lootPool` **завершённого уровня** до перехода. Вес предмета относительный. Сам предмет определяется до прокрутки; декоративные иконки ленты не задают вероятности. Сейчас за переход выдаётся одна награда: количество закреплено в коде `LevelTransitionCoordinator.RewardCount`. Скорости анимации — отдельная глава в руководстве аниматора.

В `DungeonLevelConfig → Loot Pool → Entries` поле `item` выбирает определение награды, `weight` — относительный вес, `currencyAmount` — количество золота или шардов (BigDouble, положительное целое; дробная величина округляется). Для зелий и свитков `currencyAmount` игнорируется: выдаётся один предмет. Записи без предмета, с неположительным/неконечным весом или недопустимой суммой валюты не участвуют в выборе. Пул должен содержать хотя бы одну допустимую награду.

Валюта начисляется сразу на общий баланс, без ячейки инвентаря. Зелье богатства её не умножает. Определения: [GoldReward](/Users/admin/IncrementalRPG/Assets/SO/Items/GoldReward.asset), [ShardReward](/Users/admin/IncrementalRPG/Assets/SO/Items/ShardReward.asset). Название и иконка задаются в этих ассетах; суммы — отдельно в каждом уровне.

| Пул завершённого уровня | Золото за выпадение | Шарды за выпадение | Вес золота | Вес шардов | Суммарный вес свитков |
|---|---:|---:|---:|---:|---:|
| 01 | 50 | 5 | 20 | 15 | 30 |
| 02 | 100 | 8 | 20 | 15 | 35 |
| 03 | 150 | 12 | 20 | 15 | 40 |
| 04 | 250 | 18 | 20 | 15 | 43 |
| 05 | 400 | 25 | 20 | 15 | 45 |

Остальной вес приходится на зелья из таблицы выше. Сумма весов каждого текущего пула — 100; при изменении одного веса вероятности всех наград пересчитываются относительно новой суммы. Пул AnyBiome использует значения уровня 01, но сам биом не включён в `DungeonList`. На конце демо обычный лутбокс перехода не выдаётся.

<a id="scroll-rewards"></a>

### Свитки в наградах

Все шесть определений имеют `category = Scroll`, размер 1×1, `stackable = true`, `maxStackSize = 99`, `sellPrice = 5`. Они сохраняются в инвентаре; кнопка «Применить» в лутбоксе для них скрыта. Свиток выбирается справа в окне кузницы и расходуется при успешном запуске ковки. За одну попытку используется не более одного свитка.

| Ассет | `forgeModifierId` | `forgeModifierValue` | Назначение | Веса в пулах 01 / 02 / 03 / 04 / 05 |
|---|---|---:|---|---|
| [PrecisionScroll](/Users/admin/IncrementalRPG/Assets/SO/Items/PrecisionScroll.asset) | `qte_precision` | 0.02 | Расширение центрального диапазона QTE на 2% | 5 / 6 / 7 / 8 / 8 |
| [HelmetScroll](/Users/admin/IncrementalRPG/Assets/SO/Items/HelmetScroll.asset) | `guaranteed_helmet` | 0 | Гарантированный шлем | 5 / 6 / 7 / 7 / 8 |
| [ChestScroll](/Users/admin/IncrementalRPG/Assets/SO/Items/ChestScroll.asset) | `guaranteed_chest` | 0 | Гарантированный нагрудник | 5 / 6 / 7 / 7 / 8 |
| [WeaponScroll](/Users/admin/IncrementalRPG/Assets/SO/Items/WeaponScroll.asset) | `guaranteed_weapon` | 0 | Гарантированное оружие | 5 / 6 / 7 / 7 / 8 |
| [RarityScroll](/Users/admin/IncrementalRPG/Assets/SO/Items/RarityScroll.asset) | `guaranteed_rare` | 0 | Гарантированная редкая броня | 5 / 5 / 5 / 6 / 5 |
| [EmpowermentScroll](/Users/admin/IncrementalRPG/Assets/SO/Items/EmpowermentScroll.asset) | `stat_strength` | 0.05 | Усиление всех бонусов на 5% | 5 / 6 / 7 / 8 / 8 |

У точности и усиления сила задаётся долей и передаётся в описание аргументом `{0:P0}`. Для гарантированных типа/редкости числовое поле не используется. Названия и описания: `item.scroll_<вид>.name/description` в `LocalesTable` (ru-RU, en-US, zh); иконки — спрайты из `Assets/Art/UI/NEW/ассеты ковка`.

## 11. Что пока не вынесено в настройки

| Возможность | Где сейчас определяется |
|---|---|
| Стартовое золото 10, осколки 0, броня 1 | `PlayerInfo.Default` в коде |
| Коэффициенты времени 100 и 2,5 | `GameplayFeature.ResetLevelTimer` |
| Минимальный интервал атак 0,05 с | `DamageZone.MinAttackInterval` |
| Количество предметов за переход: 1 | `LevelTransitionCoordinator.RewardCount` |
| Поведение скрытия нод в два слоя | `SkillTreeService.IsVisible`; нет настройки глубины в Inspector |
| Эффект нового типа предметной характеристики | Требуется потребитель `statId`/`effectId` в логике |
| Новые виды атак/новые StatType | Нужны изменения программной логики и её подключения |

Классы и развитие шахты/казарм не следует выводить из наличия кнопок хаба. Их соответствующие состояния пока не описывают завершённые механики.

<a id="forge-settings"></a>

## Кузница

Основной конфиг: [ForgeConfig](/Users/admin/IncrementalRPG/Assets/SO/Forge/ForgeConfig.asset), назначенный в `GameSceneInstaller → Forge Config`. Кнопка `CraftButton` в панели хаба открывает `MenuCanvas/CraftView`.

`firstAvailableLocation` — номер достигнутой локации, с которой можно ковать (счёт с 1). Сейчас 2: окно доступно из хаба сразу, а ковка — при достижении второй локации. Затем каждый новый достигнутый уровень подземелья увеличивает уровень кузницы, максимум 4. Переигрывание ранней локации не снижает кузницу. Для доступа с начала игры задайте 1.

| Уровень кузницы | Шарды за попытку | Веса Common / Rare / Unique / Legendary | Полный цикл ползунка, с | Диапазоны: редкость / все статы / один стат |
|---|---:|---|---:|---|
| 1 | 5 | 60 / 30 / 9 / 1 | 2.4 | 0.06 / 0.24 / 0.60 |
| 2 | 8 | 50 / 34 / 14 / 2 | 2.2 | 0.07 / 0.26 / 0.62 |
| 3 | 12 | 40 / 37 / 19 / 4 | 2.0 | 0.08 / 0.28 / 0.64 |
| 4 | 18 | 30 / 40 / 24 / 6 | 1.8 | 0.09 / 0.30 / 0.66 |

В `levels[]` настраиваются `shardCost`, четыре `rarityWeights`, `cursorPeriod` и диапазоны QTE. Веса относительные, конечные и неотрицательные; хотя бы один должен быть положительным. Цена — положительное целое. Диапазоны задают **полную ширину** симметричной зоны по центру шкалы: `rarityRange ≤ allStatsRange ≤ oneStatRange ≤ 1`. Например, 0.06 занимает 6% всей шкалы. За пределами `oneStatRange` дополнительного усиления нет. `oneStatBonus = 0.15` умножает один случайный бонус на 1.15; `allStatsBonus = 0.10` умножает все на 1.10. В центре редкость повышается на одну ступень и добавляется новый стат. Для уже легендарного предмета центральное попадание усиливает все статы на `allStatsBonus`.

`statRanges` задаёт минимальную и максимальную силу каждого стата по редкости. Текущие диапазоны: Common 3–7%, Rare 6–12%, Unique 10–18%, Legendary 15–25%. Статы выбираются из семи поддерживаемых характеристик без повторов, с одинаковыми шансами. Количество фиксировано: 2 / 3 / 4 / 5. Классовые статы пока не используются. При повышении редкости QTE прежние значения сохраняются, дополнительный стат берётся из диапазона новой редкости.

`equipment` содержит ровно три определения: [шлем](/Users/admin/IncrementalRPG/Assets/SO/Forge/Forgedhelmet.asset), [нагрудник](/Users/admin/IncrementalRPG/Assets/SO/Forge/Forgedchest.asset), [оружие](/Users/admin/IncrementalRPG/Assets/SO/Forge/Forgedweapon.asset). Без свитка шансы типов равны. В каждом определении `forgeNameKeys` — ключи локализованных названий в `LocalesTable`, `forgeIcons` — варианты иконок. Имя и иконка выбираются независимо друг от друга, редкости и уровня кузницы. Сейчас для каждого типа три имени и один готовый арт. Не переставляйте существующие иконки в массиве: сохранённый предмет хранит индекс; новые варианты добавляйте в конец. Все определения должны оставаться в `ItemCatalog`.

Свиток редкости задаёт именно Rare до QTE. Свиток усиления умножает все бонусы на 1.05, включая добавленный после QTE стат. Свиток точности добавляет 0.02 к полной ширине центрального диапазона (два процентных пункта); ширина ограничена `allStatsRange`. Выбор свитка не расходует предмет; повторное нажатие или нажатие выбранного свитка в центральной ячейке отменяет выбор.

Для начала требуется свободный квадрат 2×2, даже при гарантированном шлеме. Шлем занимает 1×1, нагрудник — 2×2, оружие — 1×2. При нехватке места, шардов или выбранного свитка ресурсы не расходуются. Оплаченная попытка и незабранный результат сохраняются. После перезапуска не требуется платить повторно.

`goldPerBonusPercent` задаёт золото при разбивании и последующей продаже предмета в инвентаре. Сейчас 2: сумма всех бонусов в процентах умножается на 2 и округляется вниз. Например, +5% и +7% стоят 24 золота. Усиления свитка и QTE учитываются. «Взять» переносит готовый экземпляр в инвентарь, «Разбить» начисляет золото; повторно получить результат нельзя.
