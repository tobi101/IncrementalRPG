namespace UI
{
    public sealed class DemoEndPopupProvider
    {
        public DemoEndPopupView View { get; }

        public DemoEndPopupProvider(DemoEndPopupView view)
        {
            View = view;
        }
    }
}
